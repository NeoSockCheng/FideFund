import 'package:flutter/material.dart';

//charity profile
class CharityProfilePage extends StatelessWidget {
  CharityProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Charity Profile"));
  }
}
