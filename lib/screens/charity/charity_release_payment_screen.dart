//charity release payment
import 'package:flutter/material.dart';

//charity profile
class CharityReleasePaymentPage extends StatelessWidget {
  CharityReleasePaymentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Charity Release Payment"));
  }
}
